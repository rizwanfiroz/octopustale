## Packages
framer-motion | Essential for complex animations (scroll, reveal, layout transitions)
react-intersection-observer | For scroll-triggered animations

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Tailwind Config - extend colors:
colors: {
  // We'll define these CSS variables in index.css
}
